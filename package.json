{
	"name": "@dsbunny/capdb-schema",
	"version": "3.1.6",
	"description": "Schema for CapDB APIs and media capabilities",
	"type": "module",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"author": "Steve-o <fnjordy@gmail.com>",
	"license": "MIT",
	"private": false,
	"scripts": {
		"build": "tsc",
		"clean": "rm -rf dist node_modules package-lock.json",
		"pull:upstream": "./scripts/pull-upstream.sh",
		"rebuild": "npm run clean && npm run pull:upstream && npm install && npm run build",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
		"@dsbunny/error-schema": "github:dsbunny/error-schema#a6a7a9c",
		"typescript": "^5.9.3",
		"zod": "^4.3.6"
	},
	"peerDependencies": {
		"@dsbunny/error-schema": "github:dsbunny/error-schema",
		"zod": "^4.3.0"
	}
}
